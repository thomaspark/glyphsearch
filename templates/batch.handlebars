[
  {{#each font-awesome}}
  {
    "_tags": ["font-awesome"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "fa fa-{{name}}"
  },
  {{/each}} 

  {{#each glyphicons}}
  {
    "_tags": ["glyphicons"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "glyphicon glyphicon-{{name}}"
  },
  {{/each}} 

  {{#each ionicons}}
  {
    "_tags": ["ionicons"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "ionicons ion-{{name}}"
  },
  {{/each}} 

  {{#each icomoon}}
  {
    "_tags": ["icomoon"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "icomoon icon-{{name}}"
  },
  {{/each}} 

  {{#each octicons}}
  {
    "_tags": ["octicons"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "mega-octicon octicon-{{name}}"
  },
  {{/each}}

  {{#each foundation}}
  {
    "_tags": ["foundation"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "fi-{{name}}"
  },
  {{/each}}

  {{#each devicons}}
  {
    "_tags": ["devicons"],
    "name": "{{name}}",
    "tags": "{{tags}}",
    "class": "devicons devicons-{{name}}"
  },
  {{/each}}

  {}
]
